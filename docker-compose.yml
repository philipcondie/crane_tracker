services:
  db:
    image: postgis/postgis:18-3.6
    platform: linux/amd64
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crane_tracker
    ports:
      - "5531:5432"
    volumes:
      - pgdata:/var/lib/postgresql

  test_db:
    image: postgis/postgis:18-3.6
    platform: linux/amd64
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: crane_tracker_test
    ports:
      - "5532:5432"
      
volumes:
  pgdata: